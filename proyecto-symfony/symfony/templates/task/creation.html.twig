{% extends 'base.html.twig' %}

{% block title %}
    {% if edit %}
        Editar tarea
    {% else %}
        Crear tarea
    {% endif %}
{% endblock %}

{% block body %}
    <div class="example-wrapper">
        {% if edit %}
            <h1>Editar tarea</h1>
        {% else %}
            <h1>Crear tarea</h1>
        {% endif %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
